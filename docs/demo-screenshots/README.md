# Demo screenshots

Page captures only (no browser address bar). Layout:

```
unmarked/   # clean shots
  instructor/   01–05
  learner/      06–08
  closer/       09
marked/     # same shots with arrows/circles
  instructor/
  learner/
  closer/
```

| # | File | Flow |
|---|------|------|
| 01 | `library` | Instructor library + tenant pill |
| 02 | `media-ready` | Videos table · Media ready |
| 03 | `published` | Videos table · Published |
| 04 | `assign-form` | Training assign (video + learner) |
| 05 | `assigned` | Assignments table |
| 06 | `my-training` | Learner assigned list |
| 07 | `player` | Watch modal |
| 08 | `progress` | In progress · 42% |
| 09 | `docs` | Architecture / testing closer |

`testvideo.mp4` is the clip used for the upload (copied from misc). `annotate.py` regenerates marks if needed.
