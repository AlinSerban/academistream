# `@academistream/web`

React (Vite) UI for Academistream.

## Run

From the monorepo root:

```bash
npm run web:dev
```

App: `http://localhost:5173` (Vite proxies `/api` to the API).

See the root [README](../../README.md) and [technical documentation](../../docs/TECHNICAL.md).
